# [Server]
server:
  address: 0.0.0.0
  port: 6100
  servlet:
    context-path: /smart-doc-example
    session:
      timeout: -1
  tomcat:
    max-threads: 100
    uri-encoding: UTF-8
# [Spring]
spring:
  application:
    name: smart-doc-example
  main:
    banner-mode: console